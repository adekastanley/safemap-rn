import { StyleSheet, Text, View } from "react-native";
const exploreCards = () => {
	return (
		<View>
			<Text>exploreCards</Text>
		</View>
	);
};
export default exploreCards;
const styles = StyleSheet.create({});
